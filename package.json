{
  "dependencies": {
    "@slack/client": "^4.8.0",
    "aws-sdk": "^2.384.0",
    "dotenv": "^6.2.0",
    "request": "^2.88.0",
    "request-promise": "^4.2.2"
  },
  "private": true,
  "scripts": {
    "build": "docker-compose run build && docker-compose down",
    "plan": "docker-compose run terraform plan && docker-compose down",
    "apply": "docker-compose run terraform apply && docker-compose down",
    "test": "docker-compose run terraform plan"
  }
}
