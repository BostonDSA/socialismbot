{
  "dependencies": {
    "@slack/client": "^4.8.0",
    "aws-sdk": "^2.554.0",
    "googleapis": "^36.0.0",
    "moment-timezone": "^0.5.27",
    "request": "^2.88.0",
    "request-promise": "^4.2.2",
    "slackend": "^1.3.2"
  },
  "devDependencies": {
    "dotenv": "^6.2.0"
  },
  "name": "socialismbot",
  "private": true,
  "scripts": {
    "build": "docker-compose run --rm build",
    "plan": "docker-compose run --rm terraform plan",
    "apply": "docker-compose run --rm terraform apply",
    "test": "docker-compose run --rm terraform plan"
  },
  "version": "2019.3.16"
}
