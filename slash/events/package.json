{
  "name": "slack-events",
  "dependencies": {
    "dotenv": "^6.1.0",
    "googleapis": "^32.0.0",
    "moment-timezone": "^0.5.21"
  },
  "devDependencies": {
    "aws-sdk": "^2.361.0"
  },
  "scripts": {
    "package": "docker-compose run --rm package"
  }
}
